global:
  scrape_interval: 60s
  storage:
    tsdb:
      retention:
        time: 15d

scrape_configs:
  - job_name: "checkbox-backend"
    static_configs:
      - targets: ["app:8080"]
  - job_name: "node"
    static_configs:
      - targets: ["node-exporter:9100"]
